<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amir.mahem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.JobBankMenuActivity" />
        <activity android:name=".Activities.OffFinderActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.CodeVerificationActivity" />
        <activity android:name=".Activities.LawsActivity" />
        <activity android:name=".Activities.AdsActivity" />
        <activity android:name=".Activities.EmploymentActivity" />
        <activity android:name=".Activities.MyAdsActivity" />
        <activity android:name=".Activities.FavoritesActivity" />
        <activity android:name=".Activities.AboutUsActivity" />
        <activity android:name=".Activities.SettingActivity" />
        <activity android:name=".Activities.ContactUsActivity"></activity>
    </application>

</manifest>